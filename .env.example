# Database — Supabase Postgres.
# Pooled connection (used at runtime, e.g. by Netlify functions):
DATABASE_URL="postgresql://postgres.cvxdlbwokzocdxvacfgk:[YOUR-DB-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection (used only for running migrations):
DIRECT_URL="postgresql://postgres:[YOUR-DB-PASSWORD]@db.cvxdlbwokzocdxvacfgk.supabase.co:5432/postgres"

# Public site URL, used for Stripe success/cancel redirects.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Stripe (optional). Leave unset to use the built-in /checkout-demo flow.
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Transactional email (optional, for wiring up /api/leads welcome emails).
RESEND_API_KEY=""

# Supabase project (only needed if you later use the Supabase client SDK
# directly, e.g. for auth — Prisma above doesn't need these).
NEXT_PUBLIC_SUPABASE_URL="https://cvxdlbwokzocdxvacfgk.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="sb_publishable_YjuS6ZIl0S84IYrVyu0Wsg_Vkol2e0Y"
